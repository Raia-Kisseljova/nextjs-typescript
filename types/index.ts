export interface IPerson {
  name: string;
  surname: string;
  email: string;
  dob: string;
  profilePicture: string;
  theme: string;
  description: string;
}

export interface isClicked {
  isClicked: boolean;
}
